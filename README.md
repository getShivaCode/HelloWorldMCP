# hello-ascii

Minimal mcp-use server with one **`hello`** tool: optional **name** → **Hello {name}**, or omit → **Hello World**, as figlet ASCII art.

```bash
npm install
npm run dev
```

Inspector: http://localhost:3000/inspector
