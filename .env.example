# Vite (widget bundle). Set on Render at build time so prod matches your public URL.
# Dev: omit to use localhost CSP; or set if you tunnel/ngrok the same origin.
VITE_WIDGET_ORIGIN=https://helloworldmcp.onrender.com
